# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## これは何か

Age of Empires II のチーム戦を固定メンバーで記録・分析するための、ビルド不要の単一HTMLアプリです。Eloレーティング、試合履歴、AIによるチーム分け提案、プレイヤー同士の相性・直接対決成績を扱います。データ・ロジック・UIはすべて [index.html](index.html) の中に収まっています。`package.json`・ビルドツール・リンター・テストは存在しません。

## 実行方法

`index.html` をブラウザで直接開くか、静的サーバーで配信するだけです。

```bash
python -m http.server 8000
```

install/build/lint/test に相当するコマンドはこのリポジトリには存在しません。

## アーキテクチャ

### 実行の仕組み(編集時に重要)

このアプリはバンドラーや開発サーバーを使いません。`<head>` 内で React 18 と Babel Standalone を `unpkg.com` から `<script>` タグで読み込んでいます。実際のアプリコードは `<script type="text/plain" id="app-source">` というブロックの中に置かれています(`type="text/plain"` なのでブラウザはこれをそのまま実行しません)。ページ末尾の別の `<script>` がこのブロックの `textContent` を取り出し、ページ読み込み時に `Babel.transform` でJSX→JSに変換し、その結果を `eval` してReactルートをマウントします。**アプリのロジックを編集する際は、通常の `<script>` タグではなく `#app-source` 内のJSX/JSテキストを編集してください。** また、変換・評価に失敗した場合はキャッチされて `#root` に「起動エラー」として表示される仕組みなので、実際にページを読み込むまで構文エラーをチェックしてくれるリンター等は存在しない点に注意してください。

### データフロー

1. **シードデータ**(`#app-source` 冒頭):`SEED_PLAYERS`、`SEED_MATCHES`、`SEED_TEAMMATE`、`SEED_H2H`、`CHATGPT_RATING` — 過去約221戦分の実績と、手動での「各プレイヤーの強さ」評価をハードコードしたJSONです。これは毎回の再計算の出発点であり、気軽に編集すべきものではありません。
2. **ユーザーが記録した試合**は `loadMatches()` / `saveMatches()` を通じて読み書きされ、Google Apps Script のWebアプリエンドポイント(`#app-source` 下部の `GAS_URL`)経由でGoogleスプレッドシートにJSONをPOST/GETします。これにより無料でデータを端末・訪問間で永続化できています。`GAS_URL` が未設定の場合は `window.storage`(Claude Artifactストレージ、Pro以上が必要)にフォールバックします。
3. **`computeAggregates(userMatches)`** が派生状態の唯一の情報源です。レンダリングのたびに `SEED_MATCHES` + ユーザーの試合から `players`(elo/games/wins/losses/scoreHistory)、`teammate`(ペアごとの勝率)、`h2h`(直接対決成績)を算出し直します。レート(`elo`)はEloの差分更新ではなく、`estimateRatings` が毎回ゼロから推定します(下記)。
4. **AIプレイヤー**(`AI1`〜`AI5`)は過去の試合を書き起こした際のラベルの違いに過ぎず(勝った順にラベルが付いていて偏っている)、実力差を表すものではありません。そのためレートの推定では1人の `AI`(`AI_KEY`)にまとめ、全AIが同じレートを共有します。相性の計算からもAIを含むペアは除外しています。リーダーボードでも1体の「AI」として合算表示します(`Leaderboard` の `aiRow`)。
5. **レートの推定(`estimateRatings`)**:過去221戦は試合ごとの記録がなく集計値(`SEED_PLAYERS`/`SEED_TEAMMATE`/`SEED_H2H`)しかないため、Eloを1試合ずつ流せません。そこで「チームの強さ=メンバーのレートの平均、勝率はEloの式」というモデルで、集計値と記録済みの試合をもっともよく説明するレートを最尤推定しています。
   - 集計値は、本人以外のメンバーを平均的なプレイヤー(`mu`)とみなして使う(平均チーム人数 `teamSize` ≈ 3.18 で割る)
   - 1試合が集計の中で何度も数えられているので、1試合=1回分になるよう重みを下げる(`SEED_OBS`)
   - 記録済みの試合はチーム構成が分かっているのでそのまま使う(1試合=1回分)
   - 記録済みの試合のスコアも使う。勝ったチームは全員スコアが高くなるので、「同じチームの味方と比べた総合得点の高さ」(対数)が「味方と比べたレートの高さ」に比例するというモデルで加える(`buildScoreTeams` / `fitScoreModel`)。比例係数とばらつきは、まず勝敗だけで推定したレートを使って記録済みの試合から推定する(2026/9: 相関0.76)。スコアは1試合でも勝敗よりはるかに情報が多く、役割や文明の影響も受けるので `SCORE_WEIGHT`=0.25 で割り引いている
   - `CHATGPT_RATING`(外部で一度だけ行った評価、なければ1500)を事前分布の中心にする(`RATING_PRIOR_SD`=200)。試合数の多い人はデータ優先、少ない人は評価寄り
   - 注意: 過去にメンバーの実力を見てチームを組んでいた場合、個人勝率は実力差を小さめに表す。ishi_sawada はChatGPT評価(低め)と実績(勝率67%)が大きく食い違っているが、長く離れていて最近復帰した人で、勝った試合でもスコアはほぼ毎回8人中6〜7位。強い人と組んで勝てていただけとみられるため、評価はそのまま残す(ユーザー確認済み、2026/9)。スコアを反映するようにしてからは、レートは1350前後(評価の1318に近い)

### チーム分け提案(`TeamBuilder` / `suggestSplits`)

選択された(偶数人数の)メンバーについて、2チームへの分け方を総当たりで列挙し、各案を `平均レート + 相性` で評価して、2チーム間の実力差が最も小さい上位5案を返します。予想勝率も表示します。

**相性(`pairSynergy` / `buildSynergyModel`)**:ペアで組んだときの勝率はほぼ2人の個人の強さで決まるため、単純な勝率ではなく「2人のレートから予想される勝率との差」を使います。さらにその差から全体の偏り(`globalMean`)と各人の偏り(`offset`。例: レートが低めに出ている人は誰と組んでも予想より勝つ)を取り除き、ペア固有の部分だけを相性とします。試合数が少ないほど0に寄せ(経験ベイズ、散らばり `tau` はデータから推定、下限0.03)、チーム平均レートと同じ単位に換算します。2026/9 時点では、個人の強さを除くとペア固有の相性は偶然のばらつきとほぼ区別できず、補正は最大でも±20程度です。

**直前の試合を踏まえた提案(`currentSession` / `sessionForm`)**:最後の記録から12時間以内の試合を「今日の試合」とみなし、予想より勝ったチームのレートを一時的に上げ、負けたチームを下げて提案します(`SESSION_K`=40、スコア比が大差ほど最大2倍)。記録されるレートには影響しません。今日すでに対戦した組み合わせには印を付けます。最後の記録から24時間以内なら既定でオンになります。

### スコアボードOCR(`AddMatch`)

ユーザーはAoE2の試合終了後スコアボードのスクリーンショットをアップロード/貼り付けできます。base64エンコードして同じApps Scriptエンドポイントに(`type: "ocr_extract"` として)POSTし、Googleドライブの無料OCRにかけてテキストを取得します。

`parseScoresFromOcrText` は次の手順で解析します(2026/9/22 に、名前を起点に数値を拾う方式から変更)。

1. 各行を「名前」と「数値」に振り分ける。名前は `matchToRosterName`(完全一致→名前を含む→名前の断片→レーベンシュタイン距離によるあいまい一致の順)で判定し、3文字以下の名前(`ino`/`max`/`AI1` など)は単語単位の一致のみ。数値だけの行や短い断片は名前扱いしない(以前 `st`→`stone`、`42`→`urio0042` の誤爆があった)。見出し・凡例の文言(`OCR_HEADER_WORDS`)は捨てる。**ロスターに一致しない名前は「不明なプレイヤー」として行に数える**(AIは名前が毎回変わるため。数えないと以降のスコアが1人ずつずれる)
2. 数値をすべて拾い、5個(軍事/経済/テクノロジー/社会/総合)ずつのブロックに区切る。「4項目の合計=総合」が成り立つかで区切り位置を検証し、合わなければ最大 `OCR_MAX_SKIP` 個までゴミ数値として読み飛ばして再試行する
3. 名前を出現順にブロックへ割り当てる。OCRは「名前→数値5個」の順に返すことも、「名前がまとめて並んでから数値が行順に続く」形になることもあるが、どちらもこの方式で正しく対応できる

`parseScoresFromOcrText` 自体は不明な行にスコアを割り当てません(あとで `inferTeamsAndWinner` がAIとして割り当てます。下記)。合計が合わない・名前に対応する数値がない場合は推測で埋めず、`warnings` として画面に「要確認」を出します。これは常に下書き扱いであり、UI側で保存前に必ずレビュー・修正することを求めています。

**チーム分け・勝者**はOCRの文字には出てこない(盾の中のチーム番号や王冠は画像)ため、`analyzeScoreboardImage` がcanvasでピクセルを直接解析し、`inferTeamsAndWinner` がOCRで分かった行順の名前(`parseScoresFromOcrText` の戻り値 `order`)と突き合わせます。

- 行: 左端のアイコン枠(暗い画素)が縦に並ぶ位置
- 勝者: 名前の旗の右側にある王冠(くすんだ金色の画素)の量。`CROWN_THRESHOLD`。旗の長さは名前によって変わる(特にAIは名前が長い)ので、旗の右端を行ごとに検出してからその右側だけを見る。顔写真(行の高さいっぱいの暗い矩形)の手前で打ち切る。途中で投了・切断した人には勝ってもチームの王冠が付かないため、「王冠のある人が全員同じチーム」ならそのチームを勝者とする
- チーム: 右端の盾をチームごとに形・色が違うことを利用して6x8マスの模様に要約し、似ている行同士をまとめる(番号自体は読まない)。`SHIELD_SAME_TEAM_MAX_DIFF`

画像の行数と名前の数が合わない、盾が2種類に分かれない、王冠が見つからない・両チームにある、といった場合は自動入力せず「要確認」を出します。画像の一番上の人のチームが Team A になります。

**名簿にない行(AI)には `AI1` から順に番号を振ります**(2026/9/22 に追加)。これまでの記録と同じく、勝ったチームのAIを優先して番号を振り、同じチーム内は画像の上から順です。AIは全員が同じレートを共有するので、勝敗さえ合っていれば番号の振り方は結果に影響しません。スコアもその行の値をそのまま入れます。勝者が判定できなかった場合や、名簿にない行がAIの数(5体)を超える場合は割り当てず、警告を出します。

AoE2 DE の結果画面(スコアタブ)をスコア表の部分だけ切り抜いた画像で調整しており、実画像6枚(2026/7/4、9/19、9/20×2、9/21×2。うち2枚はAI入り)のスコア・チーム分け・勝者が保存済みの記録と完全に一致し、0.7〜2倍の拡大縮小や余白の有無を変えた144パターンでも判定できることを確認済みです(0.5倍だと行を数え損ねることがあるが、その場合は自動入力しない)。回帰確認用に、これらの画像とOCR結果をローカルの `ocr-logs/sample1〜6.png/.txt` に置いてある(Git管理外)。

人数やAIの数が変わる場合も、実画像から行を抜き出して合成した画像(`ocr-logs/test3v3` / `test2v2` / `test3v3_ai2`(同じチームにAI2人)/ `test4v4_ai2`(両チームにAI1人ずつ)。合成スクリプトは `scratchpad/compose.py`)で確認済み。3v3・2v2・AIが複数いる場合でも、スコア・チーム分け・勝者を正しく判定でき、拡大縮小や余白を変えた96パターンでも問題なかった。AI名に空白区切りの数字が入る場合(例: 「ン 1 世オルブラフト」)も、混入した数値は合計チェックで読み飛ばされる。片方のチームが全員AIのときは、そのチームが空のまま返り(警告あり)、人数が足りず保存ボタンは押せないので誤った記録にはならない。

なお**名簿(`SEED_PLAYERS`)にない人間が参加した場合もAIと区別できず、AI1〜として入力されてしまう**。新しいメンバーが定着したら `SEED_PLAYERS` に追加する必要がある。

### OCR不具合の調査(デバッグログ)

OCRを実行するたびに、`AddMatch` がブラウザのIndexedDB(DB名 `aoe2_ocr_debug`、直近 `OCR_LOG_MAX`=30件)にログを記録します。IndexedDBが使えない環境では、ページを開いている間だけメモリに保持します。「試合を記録」タブの「OCRデバッグログを保存」リンクから、`aoe2-ocr-log-YYYYMMDD-HHMM.json` としてダウンロードできます。スプレッドシート側(GAS)には何も送りません。

ユーザーから「違う人のスコアが入った」「空欄になった」などの報告があったら、このJSONを `ocr-logs/` に置いてもらって調査します(`ocr-logs/` は `.gitignore` 済み)。JSONの構成は `{ exportedAt, roster, logs: [...] }` で、`logs` の各要素は次のとおりです。

- `imageDataUrl`: 貼り付けた画像そのもの(data URL)。見るときは次のコマンドでPNGに書き出してから Read します:
  `node -e "const j=require('./ocr-logs/<file>.json');j.logs.forEach(l=>l.imageDataUrl&&require('fs').writeFileSync('ocr-logs/'+l.id+'.png',Buffer.from(l.imageDataUrl.split(',')[1],'base64')))"`
- `rawText`: GAS/Googleドライブ OCR が返した生テキスト
- `trace`: `parseScoresFromOcrText` が各行をどう解釈したか(`name`(一致方法 `exact`/`partial`/`fragment`/`fuzzy`)/`unknown_name`/`number`/`ignored`/`skipped_numbers`/`leftover_numbers`/`unknown_row`/`duplicate_ignored`)
- `warnings`: 画面に出した「要確認」メッセージ
- `order`: 画面上の行順の名前
- `imageAnalysis`: 画像解析の結果(各行の `top`/`bottom`、`crown`(王冠らしさ)、`shield`(盾の模様)、`shieldBox`)
- `teamInference`: 推定したチーム分け・勝者
- `parsed`: 解析結果(`rawNums` は拾った数値そのもの。5個目は総合で、保存には使われず合計チェックにのみ使う。`warning` はその人への要確認メッセージ)
- `scoresBefore` / `scoresAfterOcr`: OCR直前の入力欄の状態と、自動入力後の状態(前回の読み取り結果が残っているとマージされる)
- 保存後に追記される `savedMatch`、`scoresAtSave`、`finalScores`、`corrections`(OCR値→最終値の差分=ユーザーが手で直した箇所)、`teamCorrected`/`winnerCorrected`(画像から推定したチーム分け・勝者を手で直したか)

`rawText` と `trace` を見れば、原因がOCR自体の誤読なのか、解析ロジック(名前の誤マッチ、数値の取りこぼしやずれ)なのかを切り分けられます。解析ロジックはブラウザで `parseScoresFromOcrText(rawText, trace = [])` を実行すれば再現できます。画像の判定は `analyzeScoreboardImage(await loadImageData(imageDataUrl))` で再現できます(`file://` で開くとcanvasが読めないので、`python -m http.server` で配信して試す)。

### 総評タブ(`PlayerReviews`)

各プレイヤー(とAI1〜5を合算した「AI」)の傾向・評価を載せたページです。文章(`REVIEW_OVERVIEW` / `PLAYER_REVIEWS`)は、`REVIEW_DATE` 時点のデータを Claude Code が分析して書いた固定テキストです。各カード下部の「最新データ」(スコア構成・相性・直接対決)は表示のたびに計算します。ユーザーから「総評を更新して」と頼まれたら、最新データ(レート、勝率、スコアの内訳と味方比、`pairSynergy`、実力から予想される勝率と比べた直接対決)を集計し直して、文章と `REVIEW_DATE` / `REVIEW_BASIS` を書き換えます。書いた数値は必ずデータと照合すること(直接対決の勝敗はどちら側から見た数字かを間違えやすい)。

## このリポジトリで見られる規約

- UI文言・コードコメント・コミットメッセージはすべて日本語です。
- コミットメッセージは短い命令調の要約(例:`スコア詳細表示改善`、`画像読み取り機能修正`)で、Conventional Commits のようなプレフィックスは使っていません。
- 色・タイポグラフィは `#app-source` 下部の `h2Style`、`cardStyle` などJSのスタイルオブジェクトとしてインラインで定義されています。CSSクラスやスタイルシートを新設するのではなく、このパターンに合わせてください。
